import { StyleSheet } from "react-native";
export const StylesNotes = StyleSheet.create({
    ContainerNotes:{
        flex: 1,
        backgroundColor: '#fff',
        justifyContent: 'center',
        alignItems: 'center',
    },
    TextNotes:{
        fontSize: 30,
        color: '#000',
    }
})